A first class function is a function that is treated like a variable. For example a higher order function that can be passed to another function as an argument.

const sum = (num1,num2) => {
return num1+num2
}
console.log(sum);
