//Declare a function called greet that has 1 argument called name and returns a String "Hi, <name>!" using arrow functions

const greet = (name) => {
    return `Hi, ${name}`
}

console.log(greet('Ed'))