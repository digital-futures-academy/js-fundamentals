function encrypt(plaintext, key) {
    // A function to encrpyt plain text based on substitution (given table of values). A key is required to indicate offset of substitutions.
}

function LetterNumber() {
    // This is a class.
    this.encrypt = encrypt;
}